# Prodigy WD Task-01 — Responsive Landing Page

Responsive landing page with an interactive fixed navbar.

## Features
- Fixed navbar with style change on scroll + hover effects
- Mobile hamburger menu (ESC/outside click close + body scroll lock)
- Scrollspy active link highlighting (IntersectionObserver)
- Smooth scrolling (respects prefers-reduced-motion)
- FAQ accordion (keyboard accessible)
- Pricing toggle (Monthly/Yearly)
- Contact form validation + success state
- Responsive layout (mobile-first)

## Run locally
```bash
python -m http.server 8000
